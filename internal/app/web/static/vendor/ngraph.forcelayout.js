import n from"./ngraph.merge.js";import e from"./ngraph.events-1.2.2.js";import t from"./ngraph.random.js";var o={exports:{}},r={exports:{}},i=function(n){return 0===n?"x":1===n?"y":2===n?"z":"c"+(n+1)};const a=i;var d=function(n){return function(e,t){let o=t&&t.indent||0,r=t&&void 0!==t.join?t.join:"\n",i=Array(o+1).join(" "),d=[];for(let t=0;t<n;++t){let n=a(t),o=0===t?"":i;d.push(o+e.replace(/{var}/g,n))}return d.join(r)}};const s=d;function u(n,e){return`\n${v(n,e)}\n${c(n)}\nreturn {Body: Body, Vector: Vector};\n`}function c(n){let e=s(n),t=e("{var}",{join:", "});return`\nfunction Body(${t}) {\n  this.isPinned = false;\n  this.pos = new Vector(${t});\n  this.force = new Vector();\n  this.velocity = new Vector();\n  this.mass = 1;\n\n  this.springCount = 0;\n  this.springLength = 0;\n}\n\nBody.prototype.reset = function() {\n  this.force.reset();\n  this.springCount = 0;\n  this.springLength = 0;\n}\n\nBody.prototype.setPosition = function (${t}) {\n  ${e("this.pos.{var} = {var} || 0;",{indent:2})}\n};`}function v(n,e){let t=s(n),o="";return e&&(o=`${t("\n   var v{var};\nObject.defineProperty(this, '{var}', {\n  set: function(v) { \n    if (!Number.isFinite(v)) throw new Error('Cannot set non-numbers to {var}');\n    v{var} = v; \n  },\n  get: function() { return v{var}; }\n});")}`),`function Vector(${t("{var}",{join:", "})}) {\n  ${o}\n    if (typeof arguments[0] === 'object') {\n      // could be another vector\n      let v = arguments[0];\n      ${t('if (!Number.isFinite(v.{var})) throw new Error("Expected value is not a finite number at Vector constructor ({var})");',{indent:4})}\n      ${t("this.{var} = v.{var};",{indent:4})}\n    } else {\n      ${t('this.{var} = typeof {var} === "number" ? {var} : 0;',{indent:4})}\n    }\n  }\n  \n  Vector.prototype.reset = function () {\n    ${t("this.{var} = ",{join:""})}0;\n  };`}r.exports=function(n,e){let t=u(n,e),{Body:o}=new Function(t)();return o},r.exports.generateCreateBodyFunctionBody=u,r.exports.getVectorCode=v,r.exports.getBodyCode=c;var f={exports:{}};const p=d,h=i;function g(n){let e=p(n),t=Math.pow(2,n);return`\n${x()}\n${b(n)}\n${l(n)}\n${m(n)}\n${y(n)}\n\nfunction createQuadTree(options, random) {\n  options = options || {};\n  options.gravity = typeof options.gravity === 'number' ? options.gravity : -1;\n  options.theta = typeof options.theta === 'number' ? options.theta : 0.8;\n\n  var gravity = options.gravity;\n  var updateQueue = [];\n  var insertStack = new InsertStack();\n  var theta = options.theta;\n\n  var nodesCache = [];\n  var currentInCache = 0;\n  var root = newNode();\n\n  return {\n    insertBodies: insertBodies,\n\n    /**\n     * Gets root node if it is present\n     */\n    getRoot: function() {\n      return root;\n    },\n\n    updateBodyForce: update,\n\n    options: function(newOptions) {\n      if (newOptions) {\n        if (typeof newOptions.gravity === 'number') {\n          gravity = newOptions.gravity;\n        }\n        if (typeof newOptions.theta === 'number') {\n          theta = newOptions.theta;\n        }\n\n        return this;\n      }\n\n      return {\n        gravity: gravity,\n        theta: theta\n      };\n    }\n  };\n\n  function newNode() {\n    // To avoid pressure on GC we reuse nodes.\n    var node = nodesCache[currentInCache];\n    if (node) {\n${function(n){let e=[];for(let o=0;o<t;++o)e.push(`${n}quad${o} = null;`);return e.join("\n")}("      node.")}\n      node.body = null;\n      node.mass = ${e("node.mass_{var} = ",{join:""})}0;\n      ${e("node.min_{var} = node.max_{var} = ",{join:""})}0;\n    } else {\n      node = new QuadNode();\n      nodesCache[currentInCache] = node;\n    }\n\n    ++currentInCache;\n    return node;\n  }\n\n  function update(sourceBody) {\n    var queue = updateQueue;\n    var v;\n    ${e("var d{var};",{indent:4})}\n    var r; \n    ${e("var f{var} = 0;",{indent:4})}\n    var queueLength = 1;\n    var shiftIdx = 0;\n    var pushIdx = 1;\n\n    queue[0] = root;\n\n    while (queueLength) {\n      var node = queue[shiftIdx];\n      var body = node.body;\n\n      queueLength -= 1;\n      shiftIdx += 1;\n      var differentBody = (body !== sourceBody);\n      if (body && differentBody) {\n        // If the current node is a leaf node (and it is not source body),\n        // calculate the force exerted by the current node on body, and add this\n        // amount to body's net force.\n        ${e("d{var} = body.pos.{var} - sourceBody.pos.{var};",{indent:8})}\n        r = Math.sqrt(${e("d{var} * d{var}",{join:" + "})});\n\n        if (r === 0) {\n          // Poor man's protection against zero distance.\n          ${e("d{var} = (random.nextDouble() - 0.5) / 50;",{indent:10})}\n          r = Math.sqrt(${e("d{var} * d{var}",{join:" + "})});\n        }\n\n        // This is standard gravitation force calculation but we divide\n        // by r^3 to save two operations when normalizing force vector.\n        v = gravity * body.mass * sourceBody.mass / (r * r * r);\n        ${e("f{var} += v * d{var};",{indent:8})}\n      } else if (differentBody) {\n        // Otherwise, calculate the ratio s / r,  where s is the width of the region\n        // represented by the internal node, and r is the distance between the body\n        // and the node's center-of-mass\n        ${e("d{var} = node.mass_{var} / node.mass - sourceBody.pos.{var};",{indent:8})}\n        r = Math.sqrt(${e("d{var} * d{var}",{join:" + "})});\n\n        if (r === 0) {\n          // Sorry about code duplication. I don't want to create many functions\n          // right away. Just want to see performance first.\n          ${e("d{var} = (random.nextDouble() - 0.5) / 50;",{indent:10})}\n          r = Math.sqrt(${e("d{var} * d{var}",{join:" + "})});\n        }\n        // If s / r < θ, treat this internal node as a single body, and calculate the\n        // force it exerts on sourceBody, and add this amount to sourceBody's net force.\n        if ((node.max_${h(0)} - node.min_${h(0)}) / r < theta) {\n          // in the if statement above we consider node's width only\n          // because the region was made into square during tree creation.\n          // Thus there is no difference between using width or height.\n          v = gravity * node.mass * sourceBody.mass / (r * r * r);\n          ${e("f{var} += v * d{var};",{indent:10})}\n        } else {\n          // Otherwise, run the procedure recursively on each of the current node's children.\n\n          // I intentionally unfolded this loop, to save several CPU cycles.\n${function(){let n=Array(11).join(" "),e=[];for(let o=0;o<t;++o)e.push(n+`if (node.quad${o}) {`),e.push(n+`  queue[pushIdx] = node.quad${o};`),e.push(n+"  queueLength += 1;"),e.push(n+"  pushIdx += 1;"),e.push(n+"}");return e.join("\n")}()}\n        }\n      }\n    }\n\n    ${e("sourceBody.force.{var} += f{var};",{indent:4})}\n  }\n\n  function insertBodies(bodies) {\n    ${e("var {var}min = Number.MAX_VALUE;",{indent:4})}\n    ${e("var {var}max = Number.MIN_VALUE;",{indent:4})}\n    var i = bodies.length;\n\n    // To reduce quad tree depth we are looking for exact bounding box of all particles.\n    while (i--) {\n      var pos = bodies[i].pos;\n      ${e("if (pos.{var} < {var}min) {var}min = pos.{var};",{indent:6})}\n      ${e("if (pos.{var} > {var}max) {var}max = pos.{var};",{indent:6})}\n    }\n\n    // Makes the bounds square.\n    var maxSideLength = -Infinity;\n    ${e("if ({var}max - {var}min > maxSideLength) maxSideLength = {var}max - {var}min ;",{indent:4})}\n\n    currentInCache = 0;\n    root = newNode();\n    ${e("root.min_{var} = {var}min;",{indent:4})}\n    ${e("root.max_{var} = {var}min + maxSideLength;",{indent:4})}\n\n    i = bodies.length - 1;\n    if (i >= 0) {\n      root.body = bodies[i];\n    }\n    while (i--) {\n      insert(bodies[i], root);\n    }\n  }\n\n  function insert(newBody) {\n    insertStack.reset();\n    insertStack.push(root, newBody);\n\n    while (!insertStack.isEmpty()) {\n      var stackItem = insertStack.pop();\n      var node = stackItem.node;\n      var body = stackItem.body;\n\n      if (!node.body) {\n        // This is internal node. Update the total mass of the node and center-of-mass.\n        ${e("var {var} = body.pos.{var};",{indent:8})}\n        node.mass += body.mass;\n        ${e("node.mass_{var} += body.mass * {var};",{indent:8})}\n\n        // Recursively insert the body in the appropriate quadrant.\n        // But first find the appropriate quadrant.\n        var quadIdx = 0; // Assume we are in the 0's quad.\n        ${e("var min_{var} = node.min_{var};",{indent:8})}\n        ${e("var max_{var} = (min_{var} + node.max_{var}) / 2;",{indent:8})}\n\n${function(e){let t=[],o=Array(e+1).join(" ");for(let e=0;e<n;++e)t.push(o+`if (${h(e)} > max_${h(e)}) {`),t.push(o+`  quadIdx = quadIdx + ${Math.pow(2,e)};`),t.push(o+`  min_${h(e)} = max_${h(e)};`),t.push(o+`  max_${h(e)} = node.max_${h(e)};`),t.push(o+"}");return t.join("\n")}(8)}\n\n        var child = getChild(node, quadIdx);\n\n        if (!child) {\n          // The node is internal but this quadrant is not taken. Add\n          // subnode to it.\n          child = newNode();\n          ${e("child.min_{var} = min_{var};",{indent:10})}\n          ${e("child.max_{var} = max_{var};",{indent:10})}\n          child.body = body;\n\n          setChild(node, quadIdx, child);\n        } else {\n          // continue searching in this quadrant.\n          insertStack.push(child, body);\n        }\n      } else {\n        // We are trying to add to the leaf node.\n        // We have to convert current leaf into internal node\n        // and continue adding two nodes.\n        var oldBody = node.body;\n        node.body = null; // internal nodes do not cary bodies\n\n        if (isSamePosition(oldBody.pos, body.pos)) {\n          // Prevent infinite subdivision by bumping one node\n          // anywhere in this quadrant\n          var retriesCount = 3;\n          do {\n            var offset = random.nextDouble();\n            ${e("var d{var} = (node.max_{var} - node.min_{var}) * offset;",{indent:12})}\n\n            ${e("oldBody.pos.{var} = node.min_{var} + d{var};",{indent:12})}\n            retriesCount -= 1;\n            // Make sure we don't bump it out of the box. If we do, next iteration should fix it\n          } while (retriesCount > 0 && isSamePosition(oldBody.pos, body.pos));\n\n          if (retriesCount === 0 && isSamePosition(oldBody.pos, body.pos)) {\n            // This is very bad, we ran out of precision.\n            // if we do not return from the method we'll get into\n            // infinite loop here. So we sacrifice correctness of layout, and keep the app running\n            // Next layout iteration should get larger bounding box in the first step and fix this\n            return;\n          }\n        }\n        // Next iteration should subdivide node further.\n        insertStack.push(node, oldBody);\n        insertStack.push(node, body);\n      }\n    }\n  }\n}\nreturn createQuadTree;\n\n`}function l(n){let e=p(n);return`\n  function isSamePosition(point1, point2) {\n    ${e("var d{var} = Math.abs(point1.{var} - point2.{var});",{indent:2})}\n  \n    return ${e("d{var} < 1e-8",{join:" && "})};\n  }  \n`}function y(n){var e=Math.pow(2,n);return`\nfunction setChild(node, idx, child) {\n  ${function(){let n=[];for(let t=0;t<e;++t){let e=0===t?"  ":"  else ";n.push(`${e}if (idx === ${t}) node.quad${t} = child;`)}return n.join("\n")}()}\n}`}function m(n){return`function getChild(node, idx) {\n${function(){let e=[],t=Math.pow(2,n);for(let n=0;n<t;++n)e.push(`  if (idx === ${n}) return node.quad${n};`);return e.join("\n")}()}\n  return null;\n}`}function b(n){let e=p(n),t=Math.pow(2,n);return`\nfunction QuadNode() {\n  // body stored inside this node. In quad tree only leaf nodes (by construction)\n  // contain bodies:\n  this.body = null;\n\n  // Child nodes are stored in quads. Each quad is presented by number:\n  // 0 | 1\n  // -----\n  // 2 | 3\n${function(n){let e=[];for(let o=0;o<t;++o)e.push(`${n}quad${o} = null;`);return e.join("\n")}("  this.")}\n\n  // Total mass of current node\n  this.mass = 0;\n\n  // Center of mass coordinates\n  ${e("this.mass_{var} = 0;",{indent:2})}\n\n  // bounding box coordinates\n  ${e("this.min_{var} = 0;",{indent:2})}\n  ${e("this.max_{var} = 0;",{indent:2})}\n}\n`}function x(){return"\n/**\n * Our implementation of QuadTree is non-recursive to avoid GC hit\n * This data structure represent stack of elements\n * which we are trying to insert into quad tree.\n */\nfunction InsertStack () {\n    this.stack = [];\n    this.popIdx = 0;\n}\n\nInsertStack.prototype = {\n    isEmpty: function() {\n        return this.popIdx === 0;\n    },\n    push: function (node, body) {\n        var item = this.stack[this.popIdx];\n        if (!item) {\n            // we are trying to avoid memory pressure: create new element\n            // only when absolutely necessary\n            this.stack[this.popIdx] = new InsertStackElement(node, body);\n        } else {\n            item.node = node;\n            item.body = body;\n        }\n        ++this.popIdx;\n    },\n    pop: function () {\n        if (this.popIdx > 0) {\n            return this.stack[--this.popIdx];\n        }\n    },\n    reset: function () {\n        this.popIdx = 0;\n    }\n};\n\nfunction InsertStackElement(node, body) {\n    this.node = node; // QuadTree node\n    this.body = body; // physical body which needs to be inserted to node\n}\n"}f.exports=function(n){let e=g(n);return new Function(e)()},f.exports.generateQuadTreeFunctionBody=g,f.exports.getInsertStackCode=x,f.exports.getQuadNodeCode=b,f.exports.isSamePosition=l,f.exports.getChildBodyCode=m,f.exports.setChildBodyCode=y;var w={exports:{}};w.exports=function(n){let e=B(n);return new Function("bodies","settings","random",e)},w.exports.generateFunctionBody=B;const $=d;function B(n){let e=$(n);return`\n  var boundingBox = {\n    ${e("min_{var}: 0, max_{var}: 0,",{indent:4})}\n  };\n\n  return {\n    box: boundingBox,\n\n    update: updateBoundingBox,\n\n    reset: resetBoundingBox,\n\n    getBestNewPosition: function (neighbors) {\n      var ${e("base_{var} = 0",{join:", "})};\n\n      if (neighbors.length) {\n        for (var i = 0; i < neighbors.length; ++i) {\n          let neighborPos = neighbors[i].pos;\n          ${e("base_{var} += neighborPos.{var};",{indent:10})}\n        }\n\n        ${e("base_{var} /= neighbors.length;",{indent:8})}\n      } else {\n        ${e("base_{var} = (boundingBox.min_{var} + boundingBox.max_{var}) / 2;",{indent:8})}\n      }\n\n      var springLength = settings.springLength;\n      return {\n        ${e("{var}: base_{var} + (random.nextDouble() - 0.5) * springLength,",{indent:8})}\n      };\n    }\n  };\n\n  function updateBoundingBox() {\n    var i = bodies.length;\n    if (i === 0) return; // No bodies - no borders.\n\n    ${e("var max_{var} = -Infinity;",{indent:4})}\n    ${e("var min_{var} = Infinity;",{indent:4})}\n\n    while(i--) {\n      // this is O(n), it could be done faster with quadtree, if we check the root node bounds\n      var bodyPos = bodies[i].pos;\n      ${e("if (bodyPos.{var} < min_{var}) min_{var} = bodyPos.{var};",{indent:6})}\n      ${e("if (bodyPos.{var} > max_{var}) max_{var} = bodyPos.{var};",{indent:6})}\n    }\n\n    ${e("boundingBox.min_{var} = min_{var};",{indent:4})}\n    ${e("boundingBox.max_{var} = max_{var};",{indent:4})}\n  }\n\n  function resetBoundingBox() {\n    ${e("boundingBox.min_{var} = boundingBox.max_{var} = 0;",{indent:4})}\n  }\n`}var C={exports:{}};const I=d;function _(n){return`\n  if (!Number.isFinite(options.dragCoefficient)) throw new Error('dragCoefficient is not a finite number');\n\n  return {\n    update: function(body) {\n      ${I(n)("body.force.{var} -= options.dragCoefficient * body.velocity.{var};",{indent:6})}\n    }\n  };\n`}C.exports=function(n){let e=_(n);return new Function("options",e)},C.exports.generateCreateDragForceFunctionBody=_;var S={exports:{}};const q=d;function k(n){let e=q(n);return`\n  if (!Number.isFinite(options.springCoefficient)) throw new Error('Spring coefficient is not a number');\n  if (!Number.isFinite(options.springLength)) throw new Error('Spring length is not a number');\n\n  return {\n    /**\n     * Updates forces acting on a spring\n     */\n    update: function (spring) {\n      var body1 = spring.from;\n      var body2 = spring.to;\n      var length = spring.length < 0 ? options.springLength : spring.length;\n      ${e("var d{var} = body2.pos.{var} - body1.pos.{var};",{indent:6})}\n      var r = Math.sqrt(${e("d{var} * d{var}",{join:" + "})});\n\n      if (r === 0) {\n        ${e("d{var} = (random.nextDouble() - 0.5) / 50;",{indent:8})}\n        r = Math.sqrt(${e("d{var} * d{var}",{join:" + "})});\n      }\n\n      var d = r - length;\n      var coefficient = ((spring.coefficient > 0) ? spring.coefficient : options.springCoefficient) * d / r;\n\n      ${e("body1.force.{var} += coefficient * d{var}",{indent:6})};\n      body1.springCount += 1;\n      body1.springLength += r;\n\n      ${e("body2.force.{var} -= coefficient * d{var}",{indent:6})};\n      body2.springCount += 1;\n      body2.springLength += r;\n    }\n  };\n`}S.exports=function(n){let e=k(n);return new Function("options","random",e)},S.exports.generateCreateSpringForceFunctionBody=k;var N={exports:{}};const P=d;function F(n){let e=P(n);return`\n  var length = bodies.length;\n  if (length === 0) return 0;\n\n  ${e("var d{var} = 0, t{var} = 0;",{indent:2})}\n\n  for (var i = 0; i < length; ++i) {\n    var body = bodies[i];\n    if (body.isPinned) continue;\n\n    if (adaptiveTimeStepWeight && body.springCount) {\n      timeStep = (adaptiveTimeStepWeight * body.springLength/body.springCount);\n    }\n\n    var coeff = timeStep / body.mass;\n\n    ${e("body.velocity.{var} += coeff * body.force.{var};",{indent:4})}\n    ${e("var v{var} = body.velocity.{var};",{indent:4})}\n    var v = Math.sqrt(${e("v{var} * v{var}",{join:" + "})});\n\n    if (v > 1) {\n      // We normalize it so that we move within timeStep range. \n      // for the case when v <= 1 - we let velocity to fade out.\n      ${e("body.velocity.{var} = v{var} / v;",{indent:6})}\n    }\n\n    ${e("d{var} = timeStep * body.velocity.{var};",{indent:4})}\n\n    ${e("body.pos.{var} += d{var};",{indent:4})}\n\n    ${e("t{var} += Math.abs(d{var});",{indent:4})}\n  }\n\n  return (${e("t{var} * t{var}",{join:" + "})})/length;\n`}N.exports=function(n){let e=F(n);return new Function("bodies","timeStep","adaptiveTimeStepWeight",e)},N.exports.generateIntegratorFunctionBody=F;var j=function(n,e,t,o){this.from=n,this.to=e,this.length=t,this.coefficient=o};var M=function(o){var r=j,i=n,a=e;if(o){if(void 0!==o.springCoeff)throw new Error("springCoeff was renamed to springCoefficient");if(void 0!==o.dragCoeff)throw new Error("dragCoeff was renamed to dragCoefficient")}o=i(o,{springLength:10,springCoefficient:.8,gravity:-12,theta:.8,dragCoefficient:.9,timeStep:.5,adaptiveTimeStepWeight:0,dimensions:2,debug:!1});var d=Q[o.dimensions];if(!d){var s=o.dimensions;d={Body:E(s,o.debug),createQuadTree:L(s),createBounds:T(s),createDragForce:O(s),createSpringForce:A(s),integrate:V(s)},Q[s]=d}var u=d.Body,c=d.createQuadTree,v=d.createBounds,f=d.createDragForce,p=d.createSpringForce,h=d.integrate,g=t.random(42),l=[],y=[],m=c(o,g),b=v(l,o,g),x=p(o,g),w=f(o),$=[],B=new Map,C=0;S("nbody",(function(){if(0===l.length)return;m.insertBodies(l);var n=l.length;for(;n--;){var e=l[n];e.isPinned||(e.reset(),m.updateBodyForce(e),w.update(e))}})),S("spring",(function(){var n=y.length;for(;n--;)x.update(y[n])}));var I={bodies:l,quadTree:m,springs:y,settings:o,addForce:S,removeForce:function(n){var e=$.indexOf(B.get(n));if(e<0)return;$.splice(e,1),B.delete(n)},getForces:function(){return B},step:function(){for(var n=0;n<$.length;++n)$[n](C);var e=h(l,o.timeStep,o.adaptiveTimeStepWeight);return C+=1,e},addBody:function(n){if(!n)throw new Error("Body is required");return l.push(n),n},addBodyAt:function(n){if(!n)throw new Error("Body position is required");var e=(n=>new u(n))(n);return l.push(e),e},removeBody:function(n){if(n){var e=l.indexOf(n);if(!(e<0))return l.splice(e,1),0===l.length&&b.reset(),!0}},addSpring:function(n,e,t,o){if(!n||!e)throw new Error("Cannot add null spring to force simulator");"number"!=typeof t&&(t=-1);var i=new r(n,e,t,o>=0?o:-1);return y.push(i),i},getTotalMovement:function(){return 0},removeSpring:function(n){if(n){var e=y.indexOf(n);return e>-1?(y.splice(e,1),!0):void 0}},getBestNewBodyPosition:function(n){return b.getBestNewPosition(n)},getBBox:_,getBoundingBox:_,invalidateBBox:function(){console.warn("invalidateBBox() is deprecated, bounds always recomputed on `getBBox()` call")},gravity:function(n){return void 0!==n?(o.gravity=n,m.options({gravity:n}),this):o.gravity},theta:function(n){return void 0!==n?(o.theta=n,m.options({theta:n}),this):o.theta},random:g};return function(n,e){for(var t in n)D(n,e,t)}(o,I),a(I),I;function _(){return b.update(),b.box}function S(n,e){if(B.has(n))throw new Error("Force "+n+" is already added");B.set(n,e),$.push(e)}},E=r.exports,L=f.exports,T=w.exports,O=C.exports,A=S.exports,V=N.exports,Q={};function D(n,e,t){if(n.hasOwnProperty(t)&&"function"!=typeof e[t]){var o=Number.isFinite(n[t]);e[t]=o?function(o){if(void 0!==o){if(!Number.isFinite(o))throw new Error("Value of "+t+" should be a valid number.");return n[t]=o,e}return n[t]}:function(o){return void 0!==o?(n[t]=o,e):n[t]}}}o.exports=function(n,e){if(!n)throw new Error("Graph structure cannot be undefined");var t=(e&&e.createSimulator||M)(e);if(Array.isArray(e))throw new Error("Physics settings is expected to be an object");var o=n.version>19?function(e){var t=n.getLinks(e);return t?1+t.size/3:1}:function(e){var t=n.getLinks(e);return t?1+t.length/3:1};e&&"function"==typeof e.nodeMass&&(o=e.nodeMass);var r=new Map,i={},a=0,d=t.settings.springTransform||G;a=0,n.forEachNode((function(n){p(n.id),a+=1})),n.forEachLink(g),n.on("changed",f);var s=!1,u={step:function(){if(0===a)return c(!0),!0;var n=t.step();u.lastMove=n,u.fire("step");var e=n/a<=.01;return c(e),e},getNodePosition:function(n){return m(n).pos},setNodePosition:function(n){var e=m(n);e.setPosition.apply(e,Array.prototype.slice.call(arguments,1))},getLinkPosition:function(n){var e=i[n];if(e)return{from:e.from.pos,to:e.to.pos}},getGraphRect:function(){return t.getBBox()},forEachBody:v,pinNode:function(n,e){m(n.id).isPinned=!!e},isNodePinned:function(n){return m(n.id).isPinned},dispose:function(){n.off("changed",f),u.fire("disposed")},getBody:function(n){return r.get(n)},getSpring:function(e,t){var o;if(void 0===t)o="object"!=typeof e?e:e.id;else{var r=n.hasLink(e,t);if(!r)return;o=r.id}return i[o]},getForceVectorLength:function(){var n=0,e=0;return v((function(t){n+=Math.abs(t.force.x),e+=Math.abs(t.force.y)})),Math.sqrt(n*n+e*e)},simulator:t,graph:n,lastMove:0};return z(u),u;function c(n){var e;s!==n&&(s=n,e=n,u.fire("stable",e))}function v(n){r.forEach(n)}function f(e){for(var t=0;t<e.length;++t){var o=e[t];"add"===o.changeType?(o.node&&p(o.node.id),o.link&&g(o.link)):"remove"===o.changeType&&(o.node&&h(o.node),o.link&&l(o.link))}a=n.getNodesCount()}function p(e){var o=r.get(e);if(!o){var i=n.getNode(e);if(!i)throw new Error("initBody() was called with unknown node id");var a=i.position;if(!a){var d=function(n){var e=[];if(!n.links)return e;for(var t=Math.min(n.links.length,2),o=0;o<t;++o){var i=n.links[o],a=i.fromId!==n.id?r.get(i.fromId):r.get(i.toId);a&&a.pos&&e.push(a)}return e}(i);a=t.getBestNewBodyPosition(d)}(o=t.addBodyAt(a)).id=e,r.set(e,o),y(e),function(n){return n&&(n.isPinned||n.data&&n.data.isPinned)}(i)&&(o.isPinned=!0)}}function h(n){var e=n.id,o=r.get(e);o&&(r.delete(e),t.removeBody(o))}function g(n){y(n.fromId),y(n.toId);var e=r.get(n.fromId),o=r.get(n.toId),a=t.addSpring(e,o,n.length);d(n,a),i[n.id]=a}function l(e){var o=i[e.id];if(o){var r=n.getNode(e.fromId),a=n.getNode(e.toId);r&&y(r.id),a&&y(a.id),delete i[e.id],t.removeSpring(o)}}function y(n){var e=r.get(n);if(e.mass=o(n),Number.isNaN(e.mass))throw new Error("Node mass should be a number")}function m(n){var e=r.get(n);return e||(p(n),e=r.get(n)),e}};var W=o.exports.simulator=M,z=e;function G(){}var U=o.exports;export{U as default,W as simulator};